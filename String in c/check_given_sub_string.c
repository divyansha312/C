//WAP to check whether a given substring is present in the given string
#include<stdio.h>
int main()
{
	int i=0,j=0;
	char str[30],sub_str[20];
	printf("enter the string\n");
	fgets(str,30,stdin);
	printf("enter the string to be search\n");
	fgets(sub_str,20,stdin);
	       while(sub_str!='\0')
	       {
	       	
		   }
	
	return 0;
}
